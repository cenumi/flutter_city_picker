name: city_picker
description: flutter china city picker
version: 0.0.1
homepage: 'https://github.com/cenumi/flutter_city_picker'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4


flutter:
  assets:
    - assets/

